export const alias = '/react-balkangraph-orgchart'

export const getAliasRoute = (route: string): string => `${alias}/${route}`

export const routes = {
  customConfig: 'custom-config',
  defaultTemplate: 'default-template',
  customTemplate: 'custom-template'
}
